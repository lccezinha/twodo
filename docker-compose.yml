version: '3'
services:
  db:
    image: 'postgres:9.5'
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - "5432:5432"
  web:
    build: .
    ports:
     - "8000:8888"
    depends_on:
      - 'db'
volumes:
  postgres:
